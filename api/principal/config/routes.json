{
  "routes": [
    {
      "method": "GET",
      "path": "/principals",
      "handler": "principal.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/principals/count",
      "handler": "principal.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/principals/:id",
      "handler": "principal.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/principals",
      "handler": "principal.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/principals/:id",
      "handler": "principal.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/principals/:id",
      "handler": "principal.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
